# Reminders

- Gebruik `npx mikro-orm migration:create` om migraties te maken uit entities

# TODO's

- eslint installeren zodat die de backend import naar @src/ en @shared/ omvormt (Sonnet 4 stelde eslint-plugin-import package voor)
- backend/src/config/index.ts uitsluiten van git + verwijderen van de repo
- Nakijken of er zod-achtige library bestaat die at-runtime verwerkt kunnen worden => Ajv + JSON Schema (most robust)
- Revisit the `npm version` problem
